#include "monty.h"
/**
 * nop - opcode, it doesnt do anything
 * @stack: pointer to head of stack
 * @index: the start of monty count
 * Return: nothing
 */
void nop(stack_t **stack, unsigned int index)
{
	(void) stack;
	(void) index;
}
